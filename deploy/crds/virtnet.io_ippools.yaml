---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: ippools.virtnet.io
spec:
  group: virtnet.io
  names:
    categories:
    - virtnet
    kind: IPPool
    listKind: IPPoolList
    plural: ippools
    shortNames:
    - ipp
    singular: ippool
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: gateway
      jsonPath: .spec.gateway
      name: GATEWAY
      type: string
    - description: subnet
      jsonPath: .spec.subnet
      name: SUBNET
      type: string
    - description: allocatedIPCount
      jsonPath: .status.allocatedIPCount
      name: ALLOCATED-IP-COUNT
      type: integer
    - description: unallocatedIPCount
      jsonPath: .status.unallocatedIPCount
      name: UNALLOCATED-IP-COUNT
      type: integer
    - description: totalIPCount
      jsonPath: .status.totalIPCount
      name: TOTAL-IP-COUNT
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IPPool is the Schema for the IPPool API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: IPPoolSpec defines the desired state of IPPool.
            properties:
              excludeIPs:
                items:
                  type: string
                type: array
              gateway:
                type: string
              ips:
                items:
                  type: string
                type: array
              subnet:
                type: string
            required:
            - subnet
            type: object
          status:
            description: IPPoolStatus defines the desired state of IPPool.
            properties:
              allocatedIPCount:
                format: int64
                minimum: 0
                type: integer
              allocatedIPs:
                type: string
              totalIPCount:
                format: int64
                minimum: 0
                type: integer
              unallocatedIPCount:
                format: int64
                minimum: 0
                type: integer
              unallocatedIPs:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
